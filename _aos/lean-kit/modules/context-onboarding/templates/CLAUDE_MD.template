# CLAUDE.md — {{DISPLAY_NAME}}

## Identity
**Project:** {{DISPLAY_NAME}}
**Profile:** {{PROFILE}}
**Project ID:** {{PROJECT_ID}}

## Team Model
{{TEAM_MODEL}}

## Iron Rules
{{IRON_RULES}}

## Data authority (dashboard / DB-connected projects)
When the AOS engine database is online, structured work-package and team state is **DB + API + `deploy_cascade`**; `_aos/*.yaml` files are snapshots after deploy, not the live mutation surface. Do not hand-edit `roadmap.yaml` for structured fields in that mode — use the API or route through Team 100. Hub reference: `governance/directives/ADR034_DATA_AUTHORITY_DB_SSOT_ALL_PROFILES.md`. Pure L0 projects without a running engine may still use file-based workflows per ADR034 offline/isolated-branch rules.

## Mandatory Session Reads (max 4 paths after PROJECT_CONTEXT)
{{MANDATORY_READS}}

## Boundary Constraints
### Forbidden Patterns
{{FORBIDDEN_PATTERNS}}

### Allowed Write Roots
{{ALLOWED_WRITE_ROOTS}}
