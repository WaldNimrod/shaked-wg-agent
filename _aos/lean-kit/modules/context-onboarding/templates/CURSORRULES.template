# .cursorrules — {{DISPLAY_NAME}}

You are working on **{{DISPLAY_NAME}}** (profile: {{PROFILE}}, project_id: {{PROJECT_ID}}).

## Team Model
{{TEAM_MODEL}}

## Iron Rules
{{IRON_RULES}}

## Data authority (dashboard / DB-connected)
If this project runs the dashboard with a database, structured roadmap/WP updates go through the **API** and **`deploy_cascade`**, not direct YAML edits. See hub `governance/directives/ADR034_DATA_AUTHORITY_DB_SSOT_ALL_PROFILES.md` and `methodology/AOS_CONCEPT_AND_PRINCIPLES.md` (Iron Rule #7). L0 without engine: follow ADR034 offline rules.

## Mandatory Context (read `_aos/context/PROJECT_CONTEXT.md` first; max 4 additional paths)
{{MANDATORY_READS}}

## MCP Tools
- **cursor-ide-browser** is the primary in-IDE browser/preview integration for Cursor (see `lean-kit/modules/mcp-browser/`).
- **Fallback chain** if MCP browser is unavailable: MCP browser → Selenium/Playwright → curl/snapshot-only API checks (see `MCP_BROWSER_STANDARD.md`).
- **Profile:** `mcp_profile` in `_aos/metadata.yaml` selects which MCP template is copied to `.cursor/mcp.json` when you run `generate_context.sh` / Deploy (`none` | `L0` | `L2` | `L2.5`).

## Boundary Constraints
- Forbidden patterns: {{FORBIDDEN_PATTERNS}}
- Allowed write roots: {{ALLOWED_WRITE_ROOTS}}
