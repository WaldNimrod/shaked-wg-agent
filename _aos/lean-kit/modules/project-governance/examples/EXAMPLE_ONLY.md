# EXAMPLE_ONLY

This directory contains a **fictional example project** for documentation purposes.
It is NOT a real managed project. Do NOT include it in `dashboard_config.yaml`.

The example demonstrates:
- Schema v1.1 roadmap.yaml with 2 WPs and full gate lifecycle
- team_assignments.yaml with slug-format IDs and cross-engine validation
- LOD200, LOD400, LOD500 document chain
- Track A (4-gate) L-GATE sequence
