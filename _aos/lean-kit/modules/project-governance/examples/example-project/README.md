# Example project — task-tracker (L0)

Fictional **task-tracker** scenario for the Lean Kit: one completed WP (`S001-P001-WP001`) with LOD200 → LOD400 → LOD500 chain, plus a second WP (`S001-P001-WP002`) referenced in `roadmap.yaml` at L-GATE_B.

See `../../templates/` and `../../gates/` for normative templates and checklists.

LOD SSOT (clone-relative): `../../../methodology/gate-model/LOD_STANDARD_v1.0.0.md`.
