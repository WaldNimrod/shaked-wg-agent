# AC ↔ standard_id mapping (template) v1.0.0

**Module:** standards-conventions (optional)  
**Use:** LOD400 companion table — domain product scope only.

| AC-ID | standard_id (or ADR) | SSOT path (repo-relative) |
|-------|------------------------|----------------------------|
| AC-001 | | |
| AC-002 | | |

**Rules:**
- `standard_id` must reference a document that exists in **this** repository (spoke), not the AOS hub methodology tree, unless the AC is explicitly about hub work.
- If no standard exists yet, use `PENDING` and open a spec task — do not invent hub policy here.
