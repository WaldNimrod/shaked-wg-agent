---
lod_target: LOD200
lod_status: DRAFT
track: A
authoring_team: team_110
consuming_team: team_20
date: 2026-04-12
version: v1.0.0
supersedes: null
priority: HIGH
---

# Data Field Generalization (Currency + Transit Labels) — LOD200 Concept

**work_package_id:** S005-P004-WP001
**domain:** shaked-wg-agent
**track:** A — mechanical renames, single team
**priority:** HIGH — prerequisite for any Israeli listing to flow through the pipeline
**approved_by:** _pending_
**approved_at:** _pending_

## 1. Problem Statement

The codebase has Swiss Franc (CHF) hardcoded into field names and display labels across 14+ files. `ScrapedListing.price_chf`, `SearchProfile.budget_min_chf`/`budget_max_chf`, and literal "CHF" strings in HTML reports, CLI, email notifier, API schemas, and digest builder all assume a single currency. Similarly, transit labels use "Tram lines" (Basel-specific) rather than generic "Transit lines".

Israeli listings use NIS (New Israeli Shekel). No listing from Yad2 or any other Israeli source can flow through the scoring pipeline, reporting, or notification channels until these fields are currency-agnostic.

## 2. Proposed Solution (concept only)

Rename currency-specific fields to generic names and add an explicit `currency` field. Update all consumers (scorer, CLI, HTML report, notifiers, API schemas, digest builder) to use the generic field names and display the currency from the field rather than a hardcoded string. Rename transit labels to generic terms.

## 3. Scope

### 3.1 In scope

**3.1.1 ScrapedListing field rename**

- Rename `price_chf: int | None` → `price: int | None` in `scrapers/base.py` (line 35)
- Add `currency: str = "CHF"` field to `ScrapedListing`
- All existing scrapers (wgzimmer, wg-gesucht, flatfox) set `currency="CHF"` explicitly

**3.1.2 SearchProfile field rename**

- Rename `budget_min_chf` → `budget_min` in `config.py` (line 93)
- Rename `budget_max_chf` → `budget_max` in `config.py` (line 94)
- Add `currency: str = "CHF"` to `SearchProfile` or derive from `CityDefinition`

**3.1.3 Consumer updates (14 files)**

- `scorer.py`: Update budget comparison logic (line 129) to use `budget_min`/`budget_max` and `price`
- `__main__.py`: Replace "Tram lines" with "Transit lines" (line 84); update budget display
- `publisher/html_report.py`: Replace 5+ hardcoded "CHF" references with `listing.currency` or profile currency; update transit prefix "T{n}" to configurable
- `notifier/email_notifier.py`: Replace hardcoded "CHF" with currency field
- `notifier/ntfy_notifier.py`: Replace hardcoded "CHF" with currency field
- `notifier/telegram_notifier.py`: Replace hardcoded "CHF" with currency field
- `notifier/discord_notifier.py`: Replace hardcoded "CHF" with currency field
- `notifier/digest_builder.py`: Replace hardcoded "CHF" with currency field
- `api/schemas.py`: Update Pydantic schemas to use generic field names
- `runner.py`: Update any field references

**3.1.4 Data file migration**

- Update `data/profiles/default.json` field names
- Provide backward-compat fallback for existing `data/listings.json`: `listing.get("price") or listing.get("price_chf")`

**3.1.5 Transit label generalization**

- CLI: "Tram lines" → "Transit lines"
- HTML report: "T{n}" prefix → use transit type from city definition or generic "Line {n}"

### 3.2 Out of scope (explicit)

- German UI label translation (that's S005-P004-WP003)
- HTML report full i18n (that's S005-P004-WP004)
- New currency conversion logic (not needed — each market uses its own currency)
- New scraper implementations (Yad2 etc.)

## 4. Affected Components

| Component | Nature of change |
|-----------|-----------------|
| `scrapers/base.py` | modify — rename field, add currency |
| `config.py` | modify — rename fields, add currency |
| `scorer.py` | modify — update field references |
| `__main__.py` | modify — update labels and field refs |
| `publisher/html_report.py` | modify — replace hardcoded CHF (5+ places) |
| `notifier/email_notifier.py` | modify — currency field |
| `notifier/ntfy_notifier.py` | modify — currency field |
| `notifier/telegram_notifier.py` | modify — currency field |
| `notifier/discord_notifier.py` | modify — currency field |
| `notifier/digest_builder.py` | modify — currency field |
| `api/schemas.py` | modify — schema field names |
| `runner.py` | modify — field references |
| `data/profiles/` | modify — field names in JSON |
| `data/listings.json` | modify — migration + compat fallback |

## 5. Dependencies

- None (independent of Yad2 POC)
- Should be completed BEFORE any Israeli scraper is implemented

## 6. Risks

| Risk | Likelihood | Impact | Mitigation |
|------|-----------|--------|-----------|
| Existing listings.json data loss during field rename | LOW | MED | Backward-compat fallback reads both old and new field names |
| Missed CHF reference in obscure code path | LOW | LOW | grep -r "chf\|CHF" to verify completeness |
| Breaking existing Swiss scraper output | LOW | MED | Explicit `currency="CHF"` in all Swiss scrapers; test coverage |

## 7. Success Criteria (LOD200 level)

- [ ] SC-01: `price_chf` renamed to `price` + `currency` field added to ScrapedListing
- [ ] SC-02: `budget_min_chf`/`budget_max_chf` renamed to `budget_min`/`budget_max`
- [ ] SC-03: Zero hardcoded "CHF" strings remain (all use currency field)
- [ ] SC-04: `grep -ri "price_chf\|budget_min_chf\|budget_max_chf"` returns zero hits in source code
- [ ] SC-05: "Tram lines" → "Transit lines" in CLI
- [ ] SC-06: All existing tests pass after rename
- [ ] SC-07: Backward-compat fallback for existing listings data

## 8. Track Decision

- **Track A** (LOD200 → LOD400 direct): choose if all criteria met:
  - [x] Single component or pattern-following (mechanical rename across files)
  - [x] No new state machine or async coordination
  - [x] No new persisted data model (field rename only)
  - [x] Single team execution

**Decision:** TRACK_A

## 9. Gate Approval Record

| Gate | Approver | Date | Status |
|------|---------|------|--------|
| L-GATE_S (spec+auth) | team_00 | _pending_ | PENDING |
